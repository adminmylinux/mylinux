# Sourced by interactive shells ($ENV) and login shells: API keys saved by the Settings panel.
if [ -f /root/.config/mylinux/secrets.env ]; then set -a; . /root/.config/mylinux/secrets.env; set +a; fi
